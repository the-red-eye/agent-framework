# Agent Framework - Docker Compose
# Template for running teams in containers

version: '3.8'

services:
  # ═══════════════════════════════════════════════════════════
  # TEAM: ADAMASTOR (Example)
  # Uncomment and configure to use
  # ═══════════════════════════════════════════════════════════
  
  # ada-pm:
  #   image: node:22-alpine
  #   container_name: ada-pm-adamastor
  #   restart: unless-stopped
  #   working_dir: /agent
  #   volumes:
  #     - ./agents/pool/ada:/agent
  #     - ./teams/adamastor:/team
  #     - ${WORKSPACE_PATH:-./workspace}:/workspace:ro
  #   environment:
  #     - AGENT_NAME=ada
  #     - AGENT_ROLE=pm
  #     - TEAM_ID=adamastor
  #     - DISCORD_TOKEN=${ADA_DISCORD_TOKEN}
  #     - GOOGLE_API_KEY=${GOOGLE_API_KEY}
  #     - GITHUB_TOKEN=${GITHUB_TOKEN}
  #   command: npx openclaw gateway start
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3

# ═══════════════════════════════════════════════════════════
# SHARED VOLUMES
# ═══════════════════════════════════════════════════════════
volumes:
  workspace:
    driver: local

# ═══════════════════════════════════════════════════════════
# NETWORKS
# ═══════════════════════════════════════════════════════════
networks:
  default:
    name: agent-framework
